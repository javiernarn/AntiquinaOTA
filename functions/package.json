{
  "name": "occ-duty-log-functions",
  "private": true,
  "description": "Server-side scheduler that sends real push notifications (reach the phone even when the app is closed) 20 minutes before each trainee's host-client shift.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  }
}
