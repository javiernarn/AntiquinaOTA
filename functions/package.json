{
  "name": "occ-duty-log-functions",
  "private": true,
  "description": "Server-side scheduler that sends real push notifications (shift start, lunch, clock-out reminders) for the OCC Duty Log app.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "shell": "firebase functions:shell"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  }
}
